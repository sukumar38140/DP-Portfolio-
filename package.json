{
  "name": "anti-gravity-portfolio-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix sequence",
    "build": "npm run build --prefix sequence",
    "start": "npm run start --prefix sequence",
    "install:all": "npm install --prefix sequence"
  }
}
